# Agri Agent backend secrets. Git-ignored. NEVER commit this file.

# ── Server ──────────────────────────────────────────────
PORT=4000
NODE_ENV=development
# Comma-separated origins allowed by CORS (use * during local development)
CORS_ORIGINS=*

# ── MongoDB Atlas (the Farm Marketplace database) ───────
# Agri Agent only reads/writes collections prefixed agri_* in this database.
MONGODB_URI=mongodb://vaishaldsouza_db_user:Sjec123@ac-rwaweew-shard-00-00.mqsaq6e.mongodb.net:27017,ac-rwaweew-shard-00-01.mqsaq6e.mongodb.net:27017,ac-rwaweew-shard-00-02.mqsaq6e.mongodb.net:27017/farm_marketplace?ssl=true&authSource=admin&retryWrites=true&w=majority

# ── Auth ────────────────────────────────────────────────
# node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=159a5b64c34bfa6a81afab8e81dc57ba772c825a4e58a0024399ad36125c6cc707b25f1db54f08f723657a6376868d53
JWT_EXPIRES_IN=30d

# ── Farm Marketplace integration ────────────────────────
# Shared secret in BOTH directions. Must equal AGRI_AGENT_API_KEY in the
# Farm Marketplace backend/.env.
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
MARKETPLACE_API_KEY=81d5528a5df2a9f49af779e4288224767cbe57f9eabcfaecc60e4036f27a71ff
# Email of the Agri Agent account that receives marketplace orders
# (sign up in the app first, then put that email here).
MARKETPLACE_DEFAULT_AGENT_EMAIL=agent@gmail.com
# Farm Marketplace backend base URL for status callbacks, e.g. http://192.168.1.20:5000
MARKETPLACE_API_URL=http://localhost:5000
MARKETPLACE_CALLBACK_PATH=/api/integration/order-status
MARKETPLACE_TIMEOUT_MS=10000

# ── Callback worker ─────────────────────────────────────
SYNC_WORKER_ENABLED=true
SYNC_WORKER_INTERVAL_MS=15000

# ── AI assistant (optional) ─────────────────────────────
OPENAI_API_KEY=
SARVAM_API_KEY=sk_ja34rucm_Pydoocans84tQbiaIiso0B7B
